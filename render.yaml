# Render Blueprint: 정적 사이트 (빌드 없음)
# 대시보드에서 New > Blueprint → 이 레포 연결 시 자동 적용
services:
  - type: web
    name: weekly-work-schedule
    runtime: static
    plan: free
    buildCommand: ""
    staticPublishPath: .
