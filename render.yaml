# Render Blueprint: 정적 사이트 (저장소 루트 그대로 배포)
# https://dashboard.render.com → New → Blueprint → 본 레포 선택
services:
  - type: web
    name: weekly-work-schedule
    runtime: static
    buildCommand: "echo 'Static site: no build'"
    staticPublishPath: .
